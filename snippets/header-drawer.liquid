{%- comment -%}
  Zevaraz — the mobile drawer is replaced by the Ivory Editorial design.

  Dawn's own header.liquid renders 'header-drawer'. Rather than editing that
  file, this snippet hands off to 'zev-menu-drawer', which keeps Dawn's outer
  scaffolding (header-drawer element, details#Details-menu-drawer-container,
  #menu-drawer) so MenuDrawer JS, focus trapping and Escape all still work.

  To revert: restore this file from Dawn's source and the original drawer
  returns, with no other change needed.
{%- endcomment -%}
{%- render 'zev-menu-drawer' -%}
